export interface Product {
    id: string,
    name: string,
    price: number,
    image: string,
    description: string
}

export interface ShoppingCarItem {
    id: string,
    name: string,
    price: number,
    quantity: number
}